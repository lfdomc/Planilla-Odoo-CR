<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data noupdate="1">

        <!-- Categoría de permisos -->
        <record id="module_category_facial_attendance" model="ir.module.category">
            <field name="name">Reconocimiento Facial</field>
            <field name="description">Gestión de asistencias mediante reconocimiento facial</field>
            <field name="sequence">20</field>
        </record>

        <!-- Regla: empleados solo ven sus propios registros faciales -->
        <record id="facial_attendance_log_employee_rule" model="ir.rule">
            <field name="name">Empleados: ver solo propios registros faciales</field>
            <field name="model_id" ref="model_facial_attendance_log"/>
            <field name="domain_force">[
                '|',
                ('employee_id.user_id', '=', user.id),
                ('employee_id.user_id', '=', False)
            ]</field>
            <field name="groups" eval="[(4, ref('hr.group_hr_user'))]"/>
            <field name="perm_read" eval="True"/>
            <field name="perm_write" eval="False"/>
            <field name="perm_create" eval="False"/>
            <field name="perm_unlink" eval="False"/>
        </record>

        <!-- Regla: gerentes HR ven todos los registros -->
        <record id="facial_attendance_log_manager_rule" model="ir.rule">
            <field name="name">Gerentes HR: ver todos los registros faciales</field>
            <field name="model_id" ref="model_facial_attendance_log"/>
            <field name="domain_force">[(1, '=', 1)]</field>
            <field name="groups" eval="[(4, ref('hr.group_hr_manager'))]"/>
            <field name="perm_read" eval="True"/>
            <field name="perm_write" eval="True"/>
            <field name="perm_create" eval="True"/>
            <field name="perm_unlink" eval="True"/>
        </record>

    </data>
</odoo>
